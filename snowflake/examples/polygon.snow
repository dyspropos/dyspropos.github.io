system transform using svg {

  sy rule1(tf: pose2f, size: float)
  sy shape(tf: pose2f, size: float)

  start {
    poly_point(0.0, 0.0)
    poly_point(10.0, 0.0)
    poly_point(10.0, 10.0)
    poly_point(0.0, 10.0)
    polygon( style(1.0, #000000, #ffffff), identity_pose() )
  }

  
}